# Runtime-only Dockerfile using pre-built JAR
# Uses cached invest-backend image as base to avoid registry pulls when offline
FROM invest-backend:latest

USER root

# Copy the updated pre-built JAR
COPY target/*.jar app.jar

# Ensure ownership
RUN chown appuser:appgroup /app/app.jar

USER appuser
